/**
 * キャンパス — 設計 isCampus 画面（アイソメトリック学内マップ）。
 * 実描画・状態はクライアントの CampusScreen に委譲。
 */
import { CampusScreen } from '@/components/campus-screen';

export default function CampusPage() {
  return <CampusScreen />;
}
